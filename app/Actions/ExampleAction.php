<?php

namespace App\Actions;

use Closure;

/** Single-purpose action: panggil via (new ExampleAction)->handle(...) atau app(ExampleAction::class)->handle(...) */
class ExampleAction
{
    public function handle(): void
    {
        //
    }
}
